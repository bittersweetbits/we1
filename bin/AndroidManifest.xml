<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.we"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="22" />

    <!--
    (was in application tag) android:label="@string/app_name"
    is replaced by @string/new_app_name
    -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/wedding_icon"
        android:label="@string/new_app_name"
        android:theme="@style/AppBaseTheme" >

        <!--
        Splash Screen::
        requirements:
        1.animation & app logo
        2.creator's details (u1462480 farzad merzadyan)
        -->
        <activity
            android:name=".Splash"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        
        <!-- The Main/Home Activity(has no parent activity) -->
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
        </activity>
        
        <activity
            android:name=".Map"
            android:label="@string/title_activity_map">"
            
        </activity>
        
    </application>

</manifest>